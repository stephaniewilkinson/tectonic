<%# An h1 like every other page title, so the display face comes from the element rather
    than from the `brand` this had to name while it was a div. The movement's name has
    left the title: it is the first field the form below edits, so it was being said
    twice, a line apart. %>
<h1 class="mb-6 text-3xl font-semibold text-gray-900">Edit exercise</h1>

<%== render('exercises/_form') %>
