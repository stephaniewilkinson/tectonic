<%# A page title that is a div rather than a heading, so it names the display face itself
    instead of taking it from `text-3xl`. %>
<div class="brand text-3xl my-8">
  edit exercise:
  <%= @exercise[:name] %>
  <br>
</div>
<%== render('exercises/_form') %>
