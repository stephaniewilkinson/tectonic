<div class="flex min-h-full flex-col justify-center px-6 py-12 lg:px-8">
  <div class="sm:mx-auto sm:w-full sm:max-w-sm">
    <h2 class="text-center text-2xl font-bold leading-9 tracking-tight text-gray-900">Create your account</h2>
  </div>

  <div class="mt-10 sm:mx-auto sm:w-full sm:max-w-sm">
    <form method="post" class="space-y-8 rodauth" role="form" id="create-account-form" autocomplete="on">
    <%== rodauth.csrf_tag %>
      <div>
        <label for="email" class="block text-sm font-medium leading-6 text-gray-900">Email address</label>
        <div class="mt-2">
          <%# "email", the same token the sign-in form uses, and it has to be the same one:
              what a password manager saves here it offers back there only if both forms
              name the identifier the same way. An account here is only ever identified by
              its address, so "email" is what this field is. "username" was tried and
              reverted; the note on views/login.erb has the argument for it and says when
              it would be worth trying again. %>
          <input id="email" name="login" type="email" autocomplete="email" required class="block w-full rounded-md border-0 px-3 py-1.5 text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 placeholder:text-gray-400 focus:ring-2 focus:ring-inset focus:ring-lime-500 sm:text-sm sm:leading-6">
        </div>
      </div>
      <%# The password box says "new-password". It said "off", which on a sign-up form is
          the instruction not to save the password -- and Safari and iOS Keychain obey it,
          so the phone declined to remember an account it had just watched being created.
          "new-password" is the opposite instruction: no such credential exists yet, offer
          to generate one and offer to keep it.

          It carries more weight than it did when there were two boxes. This form used to
          catch a typo by asking for the password again and no longer does, and nothing
          else catches one either, because there is no password reset in this app. A
          manager that generates the password and keeps it is the whole of what stands
          between a slip here and an account nobody can open. app.rb turns Rodauth's
          confirmation requirements off to match the boxes this form actually renders. %>
      <div>
        <label for="password" class="block text-sm font-medium leading-6 text-gray-900">Password</label>
        <div class="mt-2">
          <input id="password" name="password" type="password" autocomplete="new-password" required class="block w-full rounded-md border-0 px-3 py-1.5 text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 placeholder:text-gray-400 focus:ring-2 focus:ring-inset focus:ring-lime-500 sm:text-sm sm:leading-6">
        </div>
      </div>
      <div>
        <button type="submit" class="<%= button_style %> flex w-full justify-center bg-lime-500 px-3 py-1.5 text-sm font-semibold leading-6 text-white hover:bg-lime-600">Sign up</button>
      </div>
    </form>

    <p class="mt-10 text-center text-sm text-gray-500">
      Already have an account?
      <a href="/login" class="font-semibold leading-6 text-lime-500 hover:text-lime-600">Log in</a>
    </p>
  </div>
</div>
